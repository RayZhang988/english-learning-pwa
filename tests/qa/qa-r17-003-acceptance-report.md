# QA-R17-003 本地候选验收报告

日期：2026-08-15  
基线：`2b36a5f`（验收提交另列）

## 已通过

- 全量 Vitest：199 个文件中 192 通过、7 跳过；1,238 项通过、13 跳过。
- 类型检查、lint（仅既有 warning）和生产构建通过。
- R17 内容校验通过：正式候选为 3,005 个源身份到 3,000 个发布题；供应索引为 10,913 项；复习身份锁为 11,525 aliases；构建产物预缓存 64 个课程资源。
- R3 本地隔离生产浏览器通过：首日计划从发布资源动态得出 573/903/1689 秒、合计 3,165 秒；每项仍为 900 秒有效预算；词汇跨过第 6 题、刷新恢复及离线供应均通过。

## QA-R17-003-01｜已关闭：R6 浏览器验收 CDP 超时

- 根因：09 脚本在每个断言点通过单个 `Runtime.evaluate` 执行
  `indexedDB.getAll()`，将整个数据库导出回 CDP。额外训练会话累积后，序列化和传输超过
  20 秒；这不是应用主线程死锁。
- 修复：浏览器 harness 新增按复合索引 `[namespace+key]` 的有界记录读取；R6 只读取
  `learning.engine/current-state`、`app.learning-runtime/active-plan` 和当前模块会话快照。
- 复验：同一条本地隔离链路全部通过：3/3、三个“继续训练”入口、三模块退出→刷新→恢复、
  开放式续练、主动新开轮次、跨日/离线、PWA 与 320/390 px 均通过。

## 结论

本地候选工程与浏览器门禁通过；可交由 00 推送和部署。09 未执行 push 或部署。
