# 英语学习软件：项目级工作指令

## 1. 当前状态

- `00｜项目总纲与全局要求`：总体大纲已经完成，继续负责全局范围、优先级和任务边界。
- `01` 步骤 3–7、`02`、`04`、`05`、`06`、`07`、`08` 的当前模块交付均已完成并通过验证。
- `02` 的真实任务启动回调，以及 `01` 的活动计划存取、生产事件协调器和 06/07/08 真实任务路由已经补齐；57 个测试文件、173 项测试、类型检查和生产构建通过。
- `03` 已补齐可注册的正式评估会话、v1 可恢复快照和真实 v1 `AbilityProfile` 输出；59 个测试文件、184 项测试、类型检查和生产构建通过。
- `02` 已补口语评估跟读音频播放器契约并修复 UI 测试类型错误；`03` 已补生产语音识别结果到 spoken-response 评分证据的公开转换器。
- `01` 已完成评估快照、真实档案、首日计划、正式评估与 06/07/08 路由集成；68 个测试文件、222 项测试、完整类型检查、lint、生产构建和 PWA 均通过。
- `01` 步骤 8 已完成：验证版本已推送至 `main` 并由 GitHub Pages 成功部署；首页、Manifest 和 Service Worker 均返回 HTTP 200。
- 正式 HTTPS 地址：`https://rayzhang988.github.io/english-learning-pwa/`。
- `09` 已完成当前自动化门禁：78 个测试文件、299 项测试、类型检查、lint、生产构建、课程资源校验、PWA 预缓存和发布冒烟均通过。
- 正式浏览器黑盒发现的 `QA-001` 已由 `3ae5c9f` 修复：课程 JSON 作为真实构建资源发布并进入 PWA 预缓存；正式站评估恢复与首日计划回归通过。
- 正式浏览器黑盒发现的 `QA-002` 已由 `20c373d` 修复：口语完整录音/回放降级可形成 `unscorable-practice` 终态、解锁后续任务，但不会伪造分数或更新掌握度。
- `20c373d` 已由 GitHub Pages 工作流 `30141142971` 成功部署；正式站在强制口语识别失败条件下完成评估、口语、听力、词汇、3/3 计划和刷新恢复，`QA-002` 回归退出码为 0。
- 用户真实浏览器发现的 `QA-003` 已由 `d1e9379` 修复：PWA 改为自动激活、接管客户端、清理过期预缓存并限制单页只刷新一次，不再因不可见的更新提示永久停留在旧技术底座。
- `d1e9379` 已由 GitHub Pages 工作流 `30141749051` 成功部署；同一真实 Chrome 标签页已从旧资产 `index-CGAg-Eo9.js` 切换到新资产 `index-WPTS1Fa6.js`，并进入正式水平测试入口。
- 用户真实浏览器发现的 `QA-004` 已由 `45e97e1` 修复：听力关键词听写改为同步发布草稿、串行持久化状态，旧异步结果不再把受控输入框回写成过期内容。
- `45e97e1` 已由 GitHub Pages 工作流 `30143745055` 成功部署；正式站的快速输入、立即退出、刷新恢复和立即提交检查点均通过。
- 回归中发现的词汇训练竞态 `QA-005` 已由 `4a15e1e` 修复：词汇操作串行执行，持久化期间禁止重复动作，路由只通过运行时订阅接收状态。
- `4a15e1e` 已由 GitHub Pages 工作流 `30144364133` 成功部署，正式资产为 `index-R31Brx_E.js`；正式站完整浏览器回归退出码为 0，听力 7/7、词汇 6/6、整日计划 3/3 和刷新恢复全部通过。
- 第一版新增听力对话体验要求已由 `64d884c` 实现：完整对话按台词逐句播放，人名或角色名只显示、不朗读；双 `en-US` 音色时按说话人稳定分配不同 voice，单音色时以轻微 pitch/rate 差异降级。
- `64d884c` 已由 GitHub Pages 工作流 `30146382499` 部署；正式回归随后发现 `QA-006`：恢复后的关键词听写立即提交时可能持久化旧答案。
- `QA-006` 已由 `56ca8f1` 修复并经 GitHub Pages 工作流 `30146889205` 成功部署，正式资产为 `index-DARWx41s.js`；正式站 one/two voice 两次完整 E2E 均通过，不朗读说话人标签、A/B/A 音色稳定、听写最终值、听力 7/7、词汇 6/6、整日计划 3/3 和刷新恢复均通过。
- 听力多音色自动化与桌面正式站门禁已通过；真实 iPhone 上的主观音色可辨性、系统语音自然度、后台行为和单音色降级听感仍待真机验证。
- 用户真实听感验收已否决 `64d884c` 的当前多音色方案：音色和逐句衔接明显不自然，实际可用性低于第一版。自动化参数不同不等于听感合格；该体验门禁当前为不通过，必须由 `07` 按“自然度优先”重做并由 `09` 真实听音回归。
- `07` 已由 `724565b` 撤除 speaker 变声、多 voice 自动分配和逐句 JS 队列：完整场景改为单一中性系统 voice 的一次连续正文 utterance，pitch 固定为 1，rate 只取用户选择；单句、暂停/恢复、调速和重复控制保留。
- `724565b` 与同步验收提交 `98ffb1b` 已由 GitHub Pages 工作流 `30147986654` 成功部署，正式资产为 `index-DQn2F3sQ.js`；正式生产 E2E、QA-006 听写恢复、听力 7/7、词汇 6/6、计划 3/3 和刷新恢复均通过。
- `QA-007` 已根据用户在真实设备上的听感确认关闭：`724565b` 的单一中性 voice 与连续正文播放解决了机械假声问题；该结论只关闭听力自然度缺陷，不代表完整 iPhone 清单通过。
- 用户报告底部“训练”页四个公开入口全部进入占位空页；`QA-008` 已由 `1b504c9` 补齐 02 公开入口契约、`a326f97` 接入 01 的真实评估结果与 active-plan 任务路由。
- `a326f97` 已由 GitHub Pages 工作流 `30149442712` 成功部署，正式资产为 `index-BAJtI4Qx.js`；正式完整 E2E 验证水平测试结果页、口语/听力/词汇三个真实 taskId、未轮到/完成禁用状态、计划 3/3 和刷新恢复均通过，占位命中由 4/4 降至 0/4，`QA-008` 已关闭。
- 2026-07-27 用户要求改为“单项需求串行门禁”：一条需求必须完成其全部代码、内容、测试、应用集成、部署和实际入口验收，才允许开始下一条需求。模块内部完成不等于需求完成。
- `R6｜完成每日计划后可以继续训练` 已由用户在正式入口确认关闭。当前唯一允许实施的需求为 `R7｜每个训练单元结束显示正确数/总数和正确率`；口语内容匹配、听力双语选项、关键词听写和后续结构需求继续排队等待，不得与R7同时实施。
- `a7a7a7f` 的自适应词汇测试 v2 已通过工程验证，但已经被 R1 的“分难度词库随机抽样、分阶段估算掌握比例、合计词汇量映射中文等级”方案取代。此前短暂记录的“15 个测试关卡、30/30 才升级”规则也已废止；这两套旧方案只能作为可复用技术资产，不能由 `01` 接入正式入口。
- `03` 已由 `ac94d6d` 完成 R1 子步骤：5 个阶段各 150 个全局不重复旅游英语单词，每阶段随机抽取 30 题，支持最近题目规避、固定快照恢复、任意正确率继续、3200 词估算模型、合理区间、15 级映射、schema 3 档案及 v1/v2 显式迁移。03 定向 19 个测试文件、93 项测试，全量 88 个测试文件、349 项测试、类型检查、lint、构建和 PWA 均通过。
- `04` 已由 `d94e82b` 完成 R1 子步骤：学习引擎运行时兼容 schema 1/2/3，使用合理区间下界、点估计和结果等级最低词数三者折算值中的最小值作为首日保守词汇起点；听力、口语保持 `pending-calibration` 并使用安全起点 2.5，不生成强制校准任务。全量 89 个测试文件、365 项测试、类型检查、lint、构建和 PWA 均通过。
- `02` 已由 `edc46dc` 完成 R1 子步骤：介绍、单词答题、30 题导航、提交检查、阶段结果、恢复、旧数据迁移、总结果及系统状态页面均已交付；UI 只回传交互意图，不计算答案、掌握比例、词汇量、区间或等级。全量 90 个测试文件、374 项测试、类型检查、lint、构建和 PWA 均通过。
- `01` 已由 `405f120` 完成 R1 本地集成：正式水平测试入口只注册 R1；八类页面与真实运行时、schema 3 档案、04 首日计划、R1→v2→v1 快照读取顺序、损坏记录备份和刷新恢复均已接通。150 题完整链路及 0%/20%/50%/100% 混合成绩通过，全量 94 个测试文件、391 项测试、类型检查、lint、构建和 PWA 均通过。
- `405f120` 与状态提交 `2d37305` 已由 GitHub Pages 工作流 `30240359686` 成功部署，正式资产为 `index-CT4ajse6.js`；首页、Manifest、Service Worker 均返回 HTTP 200，生产包已确认包含 R1 路由、存储键和旅游英语词汇测试代码。
- `09` 已由 `e487a4f` 完成当时版本的桌面与正式站基线验收：R1 专项 27 个文件、145 项，09 专项 5 个文件、29 项，全量 94 个文件、391 项测试和正式站黑盒均通过；真实 iPhone 仍未验收。
- 2026-07-27 用户新增 R1 必须修正的快速作答规则：未答直接“下一题”时自动记为不会；提交阶段时所有未答题自动记为不会；新增“剩余全部不会，结束测试”，确认后把当前阶段剩余题和后续阶段全部记为不会并直接生成结果。
- 上述新规则使 `e487a4f` 的通过结论成为回归基线而非当前结论。R1 重新打开；完成 03、02、01 修补并重新部署后，09 才能重新验收。
- `03` 已由 `92b7b16` 完成快速作答运行时修补：未答前进原子写入 `uncertain`、阶段提交自动补齐未答、`finishRemainingUnknown()` 生成完整五阶段结果、停止原因和旧 schema 3 向后兼容均已交付；03 定向 19 个文件、108 项测试通过。当前全量 406 项中仅有 01 的一条旧断言仍要求未答时禁用提交，该断言必须在后续 01 集成时更新，03 不得越界修改。
- `02` 已由 `4a851b2` 完成快速作答界面修补：未答“下一题”提供独立回调和可见提示，阶段检查允许存在未答题并说明提交后按不会处理，“剩余全部不会，结束测试”使用独立危险操作和二次确认界面，取消、确认及忙碌状态回调互不混淆；02 专项 12 项测试通过。当前全量 409 项中仍只有 01 的上述一条旧断言失败。
- `01` 已由 `1f3d84f` 完成快速作答应用集成：正式入口接通原子“下一题”、阶段自动补未答和提前结束二次确认，完成快照先于能力档案与首日计划保存，重复操作、写入失败、完成重试和刷新恢复均有应用层测试；独立复验 94 个测试文件、416 项测试以及类型检查、lint、生产构建、PWA 预缓存和课程资源检查全部通过。
- `1f3d84f` 与状态提交 `ff64a26` 已由 GitHub Pages 工作流 `30246015269` 成功部署，正式资产为 `index-DGYLY5lm.js`；首页、Manifest 和 Service Worker 均返回 HTTP 200，生产资源已确认包含未答下一题提示、提前结束按钮和 `remaining-marked-unknown` 完成原因。
- `09` 已完成 R1 快速作答的桌面与正式站黑盒回归：未答双击下一题只前进一题并持久化为 `uncertain`，部分提交得到正确 1 / 不确定 29 / 有效 30，提前结束取消不改状态、确认后得到完整五阶段正确 2 / 不确定 148 / 有效 150，刷新后结果、档案、首日计划及旧 schema 3 进行中状态均可恢复；R1 专项 27 个文件、170 项，09 专项 6 个文件、33 项，全量 95 个文件、420 项以及类型检查、lint、构建、课程资源和 PWA 均通过。
- 2026-07-27 用户在手机端进入正式水平测试、询问并执行重新测试流程后明确要求继续下一问题；`00` 据此关闭 R1 的当前产品需求门禁。完整 iPhone 安装态离线、后台、VoiceOver 和缓存更新仍保留为第一版最终设备验收，不得因 R1 关闭而伪造为已测。
- `04` 已由 `32b952a` 完成 R2 计划契约：`PlanTaskAccess` 公开全部可启动 taskId、逐任务 availability 和非强制推荐，`nextTaskId` 只保留为 `recommendedTaskId` 的兼容别名；三个起始选择、全部六种完成顺序、中断推荐不锁定、终态幂等和旧 schema 1 恢复均通过。04 定向 9 个文件、54 项，全量 96 个文件、431 项以及类型检查、lint、生产构建、课程资源和 PWA 全部通过。
- `02` 已由 `13094f7` 完成 R2 展示契约：“今天”和“训练”页均提供三个同时可选任务入口，删除任务编号、顺序连线和单一主按钮，推荐只显示“建议先做”，各卡片准确回传自身真实 taskId；完成、跳过、不在计划和损坏状态诚实禁用。02 定向 4 个文件、41 项及全量运行测试 96 个文件、440 项通过，320/390 px、200% 字体和读屏语义通过；全量类型检查当前只因 01 仍使用已删除的 `primaryAction/request/id` 旧契约而失败。
- `01` 已由 `463bee4` 完成 R2 应用集成：“今天”和“训练”共用 `getPlanTaskAccess()`，三个真实未完成 taskId 均可独立路由到对应模块，启动前由 `evaluatePlanTaskStart()` 再校验；旧 `primaryAction`、`resumeTaskId` 和“尚未轮到”门禁已移除。六种完成顺序、部分完成刷新恢复、推荐不锁定、重复事件和非法/已完成 taskId 拒绝均通过；独立复验全量 96 个文件、444 项以及类型检查、lint、生产构建、课程资源和 PWA 全部通过。
- `463bee4` 与状态提交 `6dfce67` 已由 GitHub Pages 工作流 `30254660989` 成功部署，正式资产为 `index-CgCA6fnf.js`；首页、Manifest 和 Service Worker 均返回 HTTP 200，生产资源包含“任选一项开始”“建议先做”和其他任务可直接开始说明，且不再包含“尚未轮到”。
- `09` 已完成 R2 桌面与正式站黑盒：两个公开页面使用相同三个真实 taskId，词汇、听力、口语均能作为首项进入正确生产路由；全部六种完成顺序在 18 个逐项刷新检查点达到一致 3/3，推荐变化不锁定、快速重复操作幂等、旧 schema 1 恢复、缺失/损坏/已完成 taskId 拒绝和 320/390 px 无横向阻断均通过。R2 外部验收 10 项、相关集成 54 项、全量 97 个文件、454 项以及类型检查、lint、构建、课程资源和 PWA 全部通过，未发现新生产缺陷。
- 2026-07-27 用户在手机实际入口确认 R2 已修复；`00` 据此关闭 R2 产品门禁。
- R3已关闭：任务标注时间、900秒连续题流、前台有效计时、后台/锁屏排除及到时完成当前题均已通过工程、正式站和用户iPhone验收。R4也已由用户确认当前iPhone连续录音、停止、回放和下一题无错误而关闭。当前转入R6，禁止继续修改R3或R4，除非用户报告新的可复现回归。
- `04` 已由 `0900cab` 完成 R3 时间算法与事件契约：内容基线公式区分固定、题量、音频和交互步骤，最近 9 个同 domain/mode/contentType 的可信样本在至少 3 次后使用稳健中位数；只有 `source=timing-segments` 且 `reliable=true` 的前台有效片段能进入个人化，普通及旧 attempt 无论数量多少均不计入。新增可恢复、幂等的 `learning.timing.segment.recorded.v1`，区分有效、后台、暂停、空闲、加载、权限及网络等待，合法不可评分练习可保留真实时间但不产生掌握证据；每日预算不再改写单项预计时长。04 定向 10 个文件、76 项，全量 98 个文件、476 项以及类型检查、lint、生产构建、课程资源和 PWA 全部通过。
- `01` 已由 `c3d3071` 完成 R3 共享计时基础设施：前台/后台、45 秒交互空闲、连续媒体 15 分钟安全切片、暂停、崩溃恢复、稳定 pending event ID 与串行保存均已实现；01 定向 6 个文件、36 项，全量 102 个文件、504 项以及类型检查、lint、生产构建、课程资源和 PWA 全部通过。
- `06` 已由 `a514f87` 完成词汇真实计时接入：答题和反馈计入，加载、持久化、后台与空闲排除，最后 timing segment 先于任务完成事件；06 定向 10 个文件、32 项，全量 105 个文件、513 项及全部工程门禁通过。
- `07` 已由 `5c27e69` 完成听力真实计时接入：仅系统语音真实 onstart/onresume 后开始，暂停、结束、错误、取消与后台立即停止；未改变已验收的单一中性 voice 和连续正文方案。07 定向 14 个文件、59 项，全量 108 个文件、528 项及全部工程门禁通过。
- `08` 已由 `0af7857` 完成口语真实计时接入：权限、网络、加载和持久化等待排除，录音及回放由真实媒体事件驱动，正常与不可评分完成均先结算 timing；08 定向 14 个文件、51 项，全量 111 个文件、543 项及全部工程门禁通过。
- `02` 已由 `1457252` 完成 R3 时长展示：入口和训练过程区分内容估算/个人速度，完成页只把 `timing-segments` 显示为实际有效练习，旧记录诚实显示无可靠用时，并提供三模块每日汇总；02 定向 2 个文件、41 项，全量 112 个文件、567 项测试及职责内门禁通过。
- `01` 已完成 R3 最终生产集成：三个正式训练入口统一注入生产计时会话，Today/Training 使用任务自身估算，模块原反馈后串接实际有效时间，旧数据不回退墙钟；1–2 个可信样本保持内容基线，3 个样本后使用稳健中位数。R3 定向 10 个文件、72 项，全量 113 个文件、578 项以及类型检查、lint、生产构建、课程资源和 PWA 全部通过。
- `01` 的 R3 最终集成已由 `9ab305a` 提交，状态由 `16b9788` 记录并推送；GitHub Pages 工作流 `30326369853` 成功部署，正式资产为 `index-CDUEKV0C.js`，首页、Manifest 和 Service Worker 均返回 HTTP 200。
- `09` 的 R3 正式站验收发现 `QA-009`：发布课程经 `projectLearningCandidates()` 投影后没有结构化 `durationBaseline`，正式首日计划仍把词汇、听力、口语全部生成为 `legacy-content-estimate=900`，Today/Training 六张卡均显示“约 15 分钟”。R3 相关集成 23 个文件、158 项通过，外部验收 6 项中 5 项通过、1 项因 QA-009 失败；全量 114 个文件、584 项中唯一失败为该缺陷。
- `01` 已由 `4e49d7f` 完成 QA-009 的严格投影能力：能读取、验证并传递 04 原生 `TaskDurationBaseline`，非法基线会被拒绝，旧 `estimatedSeconds` 与已有活动计划不会被重写。审计确认四周发布课程的84个单元均缺该字段，且听力只有文本、没有可追溯的名义音频秒数，01 无权猜测。
- `05` 已由 `b803bd1` 为84/84个发布单元补齐版本化、可复算的结构化时长基线，并新增名义 TTS 时长规则和内容漂移校验。首日词汇/听力/口语基线为123/211/181秒，合计515秒；全课程84单元合计17,566秒。该结果揭示现有课程平均每天约10.5分钟，不能再用固定900秒或每日45分钟预算掩盖。
- `06` 已由 `c86d879` 修复 `QA-010`：词汇课程身份不再错误比较动态 `estimatedSeconds` / `durationEstimate`，但仍严格校验 schema、domain、targetModuleId、learningUnitId、contentRef、难度和 tags；旧900秒任务、结构化123秒任务、恢复和完成事件均通过回归。
- `c86d879` 与 09 验收提交 `79d90b6` 已由 GitHub Pages 工作流 `30330487187` 成功部署，正式资产为 `index-Cm31haDv.js`；首页、Manifest、Service Worker 和资产均返回 HTTP 200，隔离浏览器只缓存当前 index 资产。
- `09` 已在正式站关闭 `QA-009` 与 `QA-010`：R1 生成的首日计划目标仍为2700秒，但现有真实内容计划为515秒，词汇/听力/口语分别为123/211/181秒且均来自 `structured-content`；Today/Training 使用相同 taskId 与估算，正式词汇入口正常进入0/6真实题面。R3专项26个文件、181项，09专项8个文件、49项，全量114个文件、588项以及类型检查、lint、构建、84单元内容校验和PWA全部通过。
- 用户在真实 iPhone 完成首日词汇任务只用了12秒；正式任务仅有6题。该结果证明问题不只是入口时长标签错误，而是每日任务会在目标有效时间前耗尽题目。`QA-011｜限时训练题目耗尽` 重新打开 R3。
- 用户明确撤销“固定题数完成”和“用历史速度预测完成时间”作为每日完成规则。当前规则为：词汇、听力、口语各有15分钟有效训练目标，共45分钟；目标时间未到必须持续提供题目，时间到后完成当前题再结束，不得截断正在播放、作答或录音的题目。
- `04` 已由 `53fe618` 完成 QA-011 的训练预算契约：新计划为三个模块分别建立900秒有效预算，单个内容单元完成不再等于整个任务完成；公开可恢复的剩余秒数、稳定题目ID、供应游标、短期排除集合、`finish-current-item`、内容耗尽和预算完成事件。旧计划保持旧语义，个人速度不缩短预算。04定向11个文件、82项通过；全量594项中5项旧01断言等待后续集成迁移。
- `05` 已由 `53e67bb` 完成 QA-011 的连续内容供应索引：808个稳定候选，其中词汇489、听力197、口语122；支持 domain/mode/目标难度/可恢复游标/短期排除集合，并显式区分无合格内容、近期内容全部用过和供应器故障。支持难度0–5.5的最窄范围名义容量也不少于900秒；不得把名义容量当作无限题库承诺。
- `06` 已由 `96299b4` 完成词汇连续题流：预算任务按稳定itemId逐题供应并保存cursor/排除集，刷新不重复；每题发布item-completed，只有进入finish-current-item后完成当前题才发布budget-completed；内容耗尽可恢复且不清空排除集。旧无预算计划保持六题兼容语义，06定向11个文件、37项通过。
- `07` 已由 `84445c8` 完成听力连续题流：197个候选按稳定itemId/cursor/排除集连续供应，刷新不重复；finish-current-item等待当前播放、答题和反馈自然结束后才发布budget-completed，内容耗尽可恢复且不循环。已验收的单一中性voice、听写耐久性、双语契约与有效计时保持不变，07定向15个文件、63项通过。
- `08` 已由 `04acb0d` 完成口语连续题流：122个候选支持prompt与场景固定回答，保存itemId/cursor/排除集并刷新不重复；finish-current-item等待录音、识别或不可评分降级、回放和反馈自然结束后才发布budget-completed，内容耗尽可恢复且不循环。08定向14个文件、53项通过。
- `02` 已由 `65be1e7` 完成 QA-011 展示：新预算任务入口显示“15分钟有效训练”，三个训练页统一显示目标、剩余有效时间、累计题数、内容耗尽和“时间已到，完成本题后结束”；旧无预算任务保留原估算。UI只渲染上游快照和重试意图，不自行计时、选题或完成任务，02定向6个文件、85项通过。
- `01` 已由 `ea5db75` 完成 QA-011 正常路径集成：供应索引进入生产构建、网络资源、课程校验和PWA预缓存；三个正式路由注入生产供应器和恢复预算状态；活动计划保存剩余秒数、cursor、排除集、已完成题目与待结算attempt。单次attempt不再完成预算任务，只有budget-completed完成；01专项23个文件、111项通过。
- `04` 已由 `7550588` 补齐幂等的content.recovered事件；`06`/`07`/`08` 分别由 `438d8a3`、`9fd07e4`、`be94ccd` 在重试取得新题后先恢复预算状态再展示题目，保留cursor、排除集、已完成题目和有效时间；`01` 由 `e282215` 接入生产保存与三路由状态传播，失败重放和重复事件保持幂等。
- QA-011工程集成门禁已通过：01专项23个文件、124项，R3定向21个文件、164项，类型检查、lint、生产构建、84单元、808供应候选和PWA均通过。全量656项中唯一失败为09专属的旧first-use断言。R3当前活动子步骤转入 `09`：迁移旧验收，完成本地/正式站连续供题、到时收尾、耗尽恢复、刷新及缓存回归；通过前不得部署或解锁后续需求。
- `09` 在迁移first-use验收时发现 `QA-012（S1）`：正式808供应索引中的 `speaking-scene-quiz` 无法被08口语resolver解析，导致口语预算任务首题即进入provider-failure。失败验收由 `dfbf58a` 固化；不得通过过滤该正式候选绕过。R3当前活动子步骤退回 `08` 支持两类正式口语候选，修复后09必须从同一失败用例继续。
- `QA-012` 已由 `b878965` 修复：08可解析完整122个口语候选，包括28个speaking-scene-quiz；所有提示、答案和可接受表达均引用已发布课程。`09` 已由 `25ef394` 完成本地候选验收：原6道词汇题后出现第7题且任务仍为running，刷新恢复题目、6个完成item与排除集；899/900秒收尾、三模块耗尽恢复、媒体不截断均通过。09专项9个文件56项、R3定向24个文件199项、全量122个文件666项以及全部工程门禁通过。
- R3当前活动子步骤转入 `00` 提交、推送和部署本地候选；部署后09必须在正式URL重跑QA-011、QA-012、PWA缓存和刷新恢复。正式站通过后才允许用户开始真实iPhone三项15分钟验收。
- `ff7b85f` 已由 GitHub Pages 工作流 `30341029089` 成功部署，正式资产为 `index-DuWWQrUe.js`。正式隔离Chrome已通过QA-011：Today/Training三项均为900秒有效预算，词汇跨过原6题出现第7题且刷新完整恢复；正式口语入口进入practicing，无provider-failure；首页、Manifest、Service Worker、当前资产与release smoke均通过。QA-011/012正式自动化门禁已关闭，R3当前只剩真实iPhone三项15分钟、后台/锁屏/媒体和缓存更新验收。
- 用户真实 iPhone 的升级前旧计划暴露 `QA-013（S1）`：词汇已完成 12 秒后，进入听力或口语时因生命周期写入显式 `training: undefined`，被严格 JSON-portable 仓储拒绝。`04` 已由 `1f847d3` 改为对旧任务完全省略该可选字段；`01` 已由 `c29c63a` 用真实事件仓储回归证明词汇完成记录保留、听力/口语 started+timing+attempt 可逐次保存和刷新恢复；修复已随 `ac915a3` 由 GitHub Pages 工作流 `30345631519` 成功部署，正式资产为 `index-yjGhjGzs.js`。`09` 已由 `86c216f` 在正式 URL 的两个隔离浏览器 Profile 中完成等价旧计划回归；随后用户使用原 iPhone 数据实际完成听力（38 秒）和口语（3 个固定提示），均未再出现 JSON-portable 错误，QA-013 已关闭。该旧计划仍是无 900 秒预算的兼容计划，因此这次完成不能替代新版三模块各 15 分钟的 R3 真机验收。
- 用户真实 iPhone 随后暴露 `QA-014（S1）｜旧固定题数计划未升级`：界面虽显示“约15分钟”，听力仍在7题、7秒后完成，用户确认词汇、听力、口语三项均会提前结束。根因一是 `LearningAppProvider` 只在首次挂载时初始化，iPhone 从后台恢复或跨过午夜时不会重新检查本地日期；根因二是同日无预算兼容计划会被无条件恢复。`01` 已由 `3cf373e` 补前台日期检查，由 `46091ea` 将同日缺少900秒预算或运行进度的旧计划原地升级为新计划，同时保留档案、完成单元和历史。全量123个文件、671项测试、类型检查、lint、生产构建、课程资源和PWA均通过；GitHub Pages工作流 `30415214326` 成功部署，正式资产为 `index-BW16H4eG.js`。`09` 已在正式站分别验证同实例跨日恢复和同日旧计划升级：三项均变为900秒、预算状态为running，且未清除用户数据；QA-014自动化门禁已关闭，R3仍待用户原iPhone确认。
- 用户随后在正式入口确认升级后的词汇任务显示目标15:00、剩余14:14，累计完成4题后仍持续供题，不再按旧固定题数提前完成；QA-014的用户可见故障已关闭。该截图来自桌面Chrome且只覆盖词汇进行中状态，不能替代真实iPhone上的听力、口语、后台/锁屏和“到时完成当前题再结束”门禁，因此R3整体仍保持活动。
- 用户继续进入正式听力入口时暴露 `QA-015（S1）｜连续听力跨单元身份冲突`：900秒预算已创建，但首题显示“Listening task and content unit identities do not match”。根因是07的固定课程身份校验被错误复用于连续题流；当每日计划起始单元与供应器选出的已发布听力题不同时，初始化必然失败。07已由 `840d46e` 保留固定会话的严格校验，同时允许连续题流使用其他已发布单元的题目，并保持原计划任务作为事件身份；全量123个文件、673项测试和全部工程门禁通过。GitHub Pages工作流 `30416221942` 成功部署，正式资产为 `index-B28uL1yR.js`；09在正式站构造第二日听力任务并由供应器返回第一日题目，入口进入answering、播放器可用、failure为空。QA-015自动化门禁已关闭，当前只待用户原入口确认。
- 用户已在正式入口确认QA-015修复：听力从第1题持续到累计9题，剩余有效时间14:31，超过旧版7题后仍继续提供单词辨音和关键词听写，播放器与答题页面正常；QA-015产品门禁关闭。该证据仍来自桌面Chrome，R3下一步只验收口语连续训练，随后补真实iPhone后台/锁屏与到时收尾。
- 用户在正式口语入口完成4题、剩余14:55时暴露 `QA-016（S1）｜口语跨单元失败后误走耗尽恢复`：页面显示“Speaking stream has no acknowledged exhaustion to recover”。根因与QA-015同类，08在连续题流切换到其他已发布单元时仍使用固定课程身份校验；该解析异常只保存普通content failure、没有content-exhausted事件，但“重新加载”又无条件调用耗尽恢复。08已由 `30e9728` 新增仅供连续题流使用的跨单元会话创建路径，固定课程继续严格校验；没有真实耗尽身份的失败会话会直接重建，只有已记录耗尽事件的会话才发布恢复事件。全量123个文件、676项测试和全部工程门禁通过；GitHub Pages工作流 `30416667636` 成功部署，正式资产为 `index-CvUdEiaZ.js`。09在正式站构造第二日口语任务并供应第一日提示，入口进入practicing、录音按钮可用、failure为空；QA-016自动化门禁关闭，当前只待用户原错误页面点击重新加载后确认。
- 用户已在正式入口确认QA-016修复：口语累计4题与有效时间保持，重新加载后可录音并从“Hi, I'm Maya.”继续切换到新的“Could you spell your last name?”提示，未再出现身份冲突或虚假耗尽恢复；QA-016产品门禁关闭。该证据仍来自桌面Chrome，R3现在只剩真实iPhone三模块15分钟、后台/锁屏不计时和到时完成当前题后结束的最终设备门禁。
- 用户明确确认iPhone端已经没有问题，并说明无需逐项提供截图；该确认覆盖本轮R3要求的三模块15分钟连续训练、后台/锁屏不计时和到时完成当前题后结束。`00` 据此接受真实设备验收并正式关闭 `R3｜训练时长真实性`，不得再以缺少重复截图为由重新阻塞。下一条唯一允许激活的需求为 `R4｜口语录音过程中发生错误`；R6及之后需求继续等待。
- 用户随后明确确认当前iPhone口语录音验证没有问题；现有自动化已覆盖录音、停止、回放、识别失败降级、连续题和恢复，正式站及用户原入口也已通过。`00` 据此关闭 `R4｜口语录音过程中发生错误`，不进行无依据的重复改写；下一条唯一允许激活的需求为 `R6｜完成每日计划后可以继续训练`。
- 用户已批准R6总体规则：每日计划3/3完成后，“继续训练”让用户重新选择词汇、听力或口语；每次启动一个900秒有效训练的可选训练块，允许随时退出并保存恢复点。额外训练的完成、退出、失败或跨日清理都不得回滚当日3/3完成状态，不得伪造成新的每日必做任务；额外训练仍记录真实有效时间、题目进度和学习事件。R6步骤1已完成。
- `04` 已由 `3b4fc25` 完成R6额外训练契约：新增独立于`PlanProgress`的schema-1 `ExtraTrainingSession`与事件命名空间，覆盖三模块900秒预算、退出恢复、到时完成当前题、失败、跨日、幂等、JSON存储及掌握度证据隔离；内容供应优先级为错题、到期复习、当天内容变式、新的可选内容。该状态不会创建第四项每日任务或回滚3/3。R6当前转入`05`审计并补齐内容供应对该优先级的支持；05完成前不得进入06/07/08。
- `04` 随后由 `16bed6d` 补齐R6优先题目身份：`ExtraTrainingSession`与供应请求持久化四级`priorityItemIds`，创建时校验、去重并拒绝非法ID，退出/刷新恢复不丢失，旧会话缺字段时兼容为空组；04不再只传优先级名称而让内容层猜测错题。
- `05` 已由 `f608405` 完成R6内容供应契约：正式候选仍为808条（词汇489、听力197、口语122），新增稳定`variantFamilyId`及四级优先选择校验；近期错题和到期复习按已发布itemId精确回取，当天变式与新内容仍只来自已发布课程，支持确定性cursor、排除集、优先级回退、未知item及耗尽结果。R6当前转入`06`接入词汇额外训练；06完成前不得进入07。
- `06` 在R6步骤1审计时确认现有有效计时工厂只绑定每日`planId/taskId`，若直接复用会把额外训练计时事件误投给每日计划并污染3/3，因此未修改词汇文件并退回01补共享端口。
- `01` 已由 `1adabe9` 完成R6独立计时基础设施：新增只接受真实`ExtraTrainingSession`与专用额外事件发布端口的生产计时工厂和快照仓储，复用R3前台/后台、暂停、45秒空闲、媒体生命周期、崩溃恢复、稳定事件ID与串行保存规则；输出身份只有sessionId/localDate/domain/targetModuleId/mode，不含每日planId/taskId，原每日计时API保持兼容。定向14个文件95项、全量126个文件699项及类型、lint、构建、课程资源、PWA通过。R6返回`06`继续词汇接入。
- `01` 随后由 `f487988` 将`ExtraTrainingEffectiveTimingSessionFactoryPort`与`ExtraTrainingEventSink`公开到platform公共层，app生产工厂实现该端口；feature无需也不得反向依赖app实现路径。定向14个文件96项、全量126个文件700项及全部门禁通过。
- `06` 已由 `8d4b602` 完成R6词汇额外训练运行时：新增以sessionId为身份的独立runtime、JSON仓储和专用outbox，接通05四级优先供应与01独立计时；覆盖900秒到时完成当前题、attempt/item/budget事件顺序、退出刷新恢复、耗尽恢复、provider故障隔离、失败同ID重放、重复命令幂等和每日3/3深度不变。词汇14个文件50项、全量128个文件710项及类型、lint、构建、课程资源、PWA通过。R6当前转入`07`听力额外训练接入；07完成前不得进入08。
- `07` 已由 `99e6b35` 完成R6听力额外训练运行时：新增独立sessionId仓储、runtime与专用outbox，接通四级优先供应、01独立媒体计时、900秒到时完成当前题、退出刷新恢复、耗尽恢复与失败同ID重放；保持单一中性voice、连续正文、不朗读人名及现有暂停/调速/重复/听写持久化方案不变。07定向16个文件76项、R6相关2个文件17项、全量129个文件719项及全部门禁通过。R6当前转入`08`口语额外训练接入；08完成前不得进入02。
- `08` 已由 `6aa93d4` 完成R6口语额外训练运行时：新增独立sessionId仓储、runtime与专用outbox，接通122个正式候选、四级优先供应、录音/识别/回放/不可评分降级和独立媒体计时；900秒到时不截断录音、识别或回放，支持退出恢复、耗尽恢复与失败重放，事件不含每日plan/task身份。08定向16个文件64项、R6三模块3个文件21项、全量130个文件724项及全部门禁通过。R6当前转入`02`交付继续训练选择与额外会话展示契约；02完成前不得进入01最终集成。
- `02` 已由 `16dc6e8` 完成R6界面契约：3/3完成页新增“继续训练”，模块选择器展示词汇/听力/口语的available、paused、running、completed、content-exhausted、failed、expired状态；UI只回传start(moduleId)及携带真实sessionId的resume/retry/exit意图，不生成会话或计算状态。三模块额外训练页明确15分钟有效训练、退出保存、剩余时间和到时完成当前题，完成页提供再练15分钟与返回今日完成；320/390px、200%字体、VoiceOver、忙碌禁用和防双击均覆盖。02定向7个文件101项、R6跨层9个文件68项及完整门禁通过。R6当前转入`01`最终生产集成；01完成前不得进入09。
- `06`、`07`、`08` 随后分别由 `8c7abe4`、`8c62190`、`d21ae73` 补齐公开连续前进与同会话失败恢复；`07`、`08` 又由 `2df0a03`、`873120b` 让正式供应器真实执行R6四级`priorityItemIds`，未知优先ID明确失败，不能再忽略错题/复习身份后返回普通第一题。
- `01` 已由 `f663458` 完成R6生产集成：同日3/3是唯一准入，协调器生成稳定sessionId并只投影05正式itemId；独立事件sink串行保存extra事件和掌握证据且不经过`applyPlanEvent`；三模块正式路由、02五类回调、公共计时、独立仓储/outbox、退出恢复、900秒收尾、完成后再练、跨日过期、失败重放、JSON与旧数据兼容均已接通。01范围41个文件199项、R6跨层17个文件105项、全量141个文件780项，以及类型、lint、构建、84单元、808候选、课程资源和PWA全部通过。R6当前转入`09`本地候选与正式站验收；09通过前不得部署或关闭R6。
- `09` 本地候选验收发现并由 `7193aa0` 修复三个生产缺陷：训练路由释放时直接保存浏览器原生`queueMicrotask`导致完成页出现`Illegal invocation`；额外口语到时状态在录音前未同步，反馈按钮错误显示“下一题”；额外听力自然播放结束只更新运行时而不通知React，页面永久显示“正在播放”。三处均有定向回归。
- `09` 已由 `dc05de7` 完成R6本地候选验收：外部契约10/10，全量142个文件791项、类型检查、lint、生产构建、课程资源与PWA通过；隔离生产浏览器从真实R1生成并完成每日3/3，验证三模块额外训练、快速启动幂等、退出/刷新恢复、900秒完成当前题、完成后再练、3/3不回滚、320/390px、当前Service Worker、9个课程资源预缓存及离线三模块入口。R6当前允许提交、推送和部署；部署后必须在正式URL重跑同一黑盒并由用户在真实iPhone入口确认，完成前不得关闭R6或开始R7。
- `e7b42e2` 已由GitHub Pages工作流 `30428036715` 成功部署，正式资产为`index-CSfrI9g9.js`；正式URL从空隔离Profile完整重跑R1、每日3/3、三模块额外训练、退出刷新恢复、900秒收尾、完成后再练、PWA缓存及离线入口，退出码为0。R6自动化与正式站门禁已通过，当前只待用户在原iPhone Safari/主屏幕App确认“继续训练”入口、三模块选择、退出恢复及媒体行为；确认前不得关闭R6或开始R7。
- 用户批准开发验收使用30秒测试模式，正式训练仍固定900秒。显式URL参数`?trainingTest=30`启用30倍测试计时、顶部永久警示和独立数据库`english-learning-pwa-training-test-30s`；普通URL继续使用`english-learning-pwa`，两套档案、计划、掌握证据和进度互不读取。真实生产浏览器测得30.124秒进入`finish-current-item`并完成当前词汇题，普通15分钟R6全链路随后回归通过；测试模式不得作为最终15分钟真机时长证据。
- `3720dd4` 已由GitHub Pages工作流`30437466042`成功部署，正式资产为`index-Bq-OM850.js`；正式测试URL实测30.204秒进入`finish-current-item`，只创建`english-learning-pwa-training-test-30s`。普通正式URL随后完整R6黑盒通过并继续只使用900秒规则。当前仍未修改“每日单项完成后能否立即再练”规则，该规则等待用户确认。
- 用户随后发现测试模式仍把内部900秒直接显示为15:00。`26af6a0` 已将测试模式的目标、剩余时间和额外训练文案换算为墙钟30秒显示，同时保留存储中的900秒正式预算；全量143个文件794项及全部工程门禁通过。GitHub Pages工作流`30439609560`成功部署，正式资产为`index-5i2bPJKW.js`；正式测试URL从`00:30`开始并在30.231秒进入`finish-current-item`，普通URL仍显示15分钟且使用正式数据库。
- 用户澄清R6的核心入口还必须位于底部“训练”页，而不是只在3/3完成页进入额外训练选择器。`692ff4b` 已让3/3后的词汇、听力、口语三张卡从不可点击“已完成”变为可点击“继续训练”；点击具体卡片会直接开始、恢复或重试对应模块的额外训练，仍使用独立会话且不回滚3/3。全量143个文件796项及全部工程门禁通过；GitHub Pages工作流`30440708978`成功部署，正式资产为`index-Clq8Eo2f.js`，正式隔离浏览器已验证训练页三卡直达、退出刷新恢复、三模块完成、再次训练、离线入口及3/3不变。R6仍待用户在原入口确认该训练页交互后才能关闭。
- 用户已在原入口确认R6完成。`00`据此关闭R6：每日3/3后的底部“训练”页可以直接继续词汇、听力或口语，额外训练不会回滚3/3。下一条唯一允许激活的需求为R7；除非用户报告新的可复现回归，不得继续修改R6。
- R7本地候选已完成统一精确计分底座及三模块接入：每日训练和额外训练分别持久化`correctCount`、`incorrectCount`、`unscorableCount`，正确率只以可评分题为分母；口语沿用现有内容匹配证据，`match/close`计正确、`partial/different`计错误，识别或设备失败单列且不冒充发音评分。三个结束页显示正确数/总数、正确率和无法评分数，旧记录不反推假成绩。全量144个文件、801项测试、类型检查、lint、生产构建、课程资源与PWA均通过。R7当前进入本地/正式浏览器验收；部署和用户原入口确认前不得关闭R7或开始R8。
- R7本地隔离浏览器已通过真实R1建档、每日三模块完成、额外三模块完成、退出/刷新恢复、900秒完成当前题、PWA缓存和离线入口；验收脚本逐模块核对持久化评分账本与结束页的正确数/总数、正确率及无法评分提示，未触碰用户设备数据。R7当前允许提交、推送和部署；正式URL复验通过后才交给用户原入口确认。
- `b1fca5d`已由GitHub Pages工作流`30504315326`成功部署，正式资产为`index-CqH2RGuT.js`；首页、Manifest和Service Worker均返回HTTP 200。首次正式黑盒在部署传播窗口内刷新时命中过渡缓存并把新评分字段误报为损坏，重新用全新隔离Profile和缓存破坏参数运行后完整通过：真实R1、每日三模块、额外三模块、评分账本与结束页一致、刷新恢复、到时收尾、PWA预缓存和离线入口全部通过。该瞬时失败不能伪装成产品缺陷，也不能省略记录。R7当前只待用户原入口确认评分展示；确认前不得关闭R7或开始R8。
- 用户原入口验收否决了上述R7展示结论：训练到时后首先出现的是06/07/08各自的模块完成页，该页仍只显示“已完成N道题”和“返回今日计划”；评分只放在再点击返回后的第二层路由完成页，实际首屏不可见。R7重新打开，当前只修三个模块第一完成页直接显示正确数/总数、正确率及无法评分数；修复、部署和用户复验前不得关闭R7。
- R7首完成页修补的本地候选已通过：06/07/08的模块完成态现在直接渲染本次真实结果，词汇和听力显示正确数/总数及正确率，口语额外显示无法评分数；第二层完成页继续保留同一账本。全量144个文件、801项测试、类型检查、lint、生产构建、课程资源和PWA通过；本地隔离浏览器已逐模块断言第一完成页与持久化评分一致，并重跑每日3/3、额外训练、刷新恢复和离线入口。当前允许重新部署，正式URL与用户原入口复验前R7仍为活动状态。
- `bb0eb25` 已由GitHub Pages工作流`30505057296`部署首完成页修补，正式资产为`index-WDaK1uso.js`；用户随后在听力原入口发现第二次R7验收失败：顶部累计完成9题，但首完成页只显示最后一个功能会话的`1/1`。根因是06/07/08首完成页读取模块局部结果，而不是01持久化的整项任务评分账本。R7继续保持打开；当前修补要求01把`TaskExecutionState.score`注入三个正式训练路由，首完成页必须以该整项账本为准，禁止用最后一题或最后一个内容单元覆盖累计分母。
- `5acb2d2` 已修复R7累计分母：01把当前任务持久化评分账本注入06/07/08正式路由，三个首完成页优先显示整项任务累计正确、错误和不可评分数，局部结果只保留为脱离应用层运行时的兼容降级。全量144个文件、804项测试及全部工程门禁通过；GitHub Pages工作流`30505793256`成功部署，正式资产为`index-CL8rGEel.js`，首页、Manifest、Service Worker均为HTTP 200，正式隔离浏览器30秒模式通过。R7仍待用户在累计多题的原入口确认结束页分母与顶部累计完成数一致，确认前不得关闭或开始R8。
- 用户已在原入口确认R7累计评分展示正确。`00`据此正式关闭`R7｜每个训练单元结束显示正确数/总数和正确率`：三个模块的首完成页与持久化整项任务账本一致，不再把最后一题的局部`1/1`冒充整轮结果。下一条唯一允许激活的需求为`R8｜口语显示目标文本、识别文本及有限内容匹配`；除非用户报告新的可复现回归，不得继续修改R7。
- 用户已批准后续需求顺序为`R8 → R10 → R9 → R11 → R12 → R13-A → R13-B`。`R13-A`只建立场景训练信息架构、入口、返回/切换和明确的“暂未开放”状态，不制作题库、音频、口语内容、评分、计划或AI对话；用户确认框架后，`R13-B`才允许逐模块加入真实内容，并且一个模块完整验收后才能开始下一个。
- R13原18项场景合并为11个一级模块，原场景不得丢失，只作为子场景保留：①机场出发（机场值机、安检、登机口）；②飞机上；③到达与入境（入境海关、行李提取、海关行李检查）；④货币与通信（货币兑换、电话卡、公共Wi‑Fi）；⑤城市交通（机场交通、出租车、公共交通）；⑥租车自驾；⑦酒店住宿；⑧餐厅用餐；⑨商店购物；⑩观光与公共求助（景点观光、路人求助、卫生间）；⑪医疗与药店。R13-A通过只代表框架可用，不代表场景训练内容完成。
- R8本地候选已完成：复用08既有且可恢复的`transcript`、`closestAcceptedAnswer`和`match/close/partial/different`有限文本匹配证据，不新增或伪造发音数据；每日口语和额外口语反馈页均以独立字段展示“目标表达”“实际识别”和内容匹配结论，并明确声明不是发音、口音或流利度评分。识别失败时目标表达仍可见，实际识别明确显示无可用文本，结果为无法判断且不记为答错。全量145个文件、809项测试、类型检查、lint、生产构建、课程资源和PWA均通过；本地隔离浏览器的可控识别成功及识别不可用两条完整链路均通过每日3/3、额外训练、900秒收尾、计分、刷新恢复与离线入口。R8当前允许提交、推送和部署；正式URL及用户原入口复验前不得关闭R8或开始R10。
- `7201f29` 已由GitHub Pages工作流`30507417147`成功部署R8，正式资产为`index-BorgP2yS.js`，首页、Manifest和Service Worker均返回HTTP 200。正式URL已用两个全新隔离Profile并行通过可控识别成功与识别不可用链路：每日口语和额外口语均展示结构化目标/识别文本或诚实的无文本状态，同时保持每日3/3、额外训练、计分、900秒收尾、刷新恢复、PWA预缓存和离线入口。R8自动化与正式站门禁通过，当前只待用户在原iPhone入口确认系统实际识别文本和有限内容反馈；确认前不得关闭R8或开始R10。
- 自动化与桌面真实浏览器的既有功能门禁已经通过；第一版最终结论仍待本轮修订、真实 iPhone Safari/主屏幕 Web App 验证，以及连续 14 天个人使用记录。
- 下一步先按本文第 3.5 节完成第一版修订；修订部署并通过自动化后，再按 `tests/qa/iphone-checklist.md` 完成安装、离线、麦克风、录音、系统语音、VoiceOver、后台中断、缓存更新和恢复；真机门禁通过后，才开始填写 `tests/qa/14-day-usage-log.md`。
- 开放式 AI 对话属于第二阶段，第一版不得实现。

## 2. 第一版总目标

开发一个供单人使用的英语学习 PWA：

- Mac 开发，iPhone Safari 或主屏幕 Web App 使用。
- 不登录、无云同步、无付费在线服务。
- 学习数据仅保存在当前设备。
- 词汇和听力内容下载后可离线使用。
- 听力对话的人名或角色名只在界面显示，不得作为台词反复朗读。播放体验以自然度、清晰度和连贯性优先于人物音色区分：只有设备存在两个经真实听感验证可接受的 `en-US` 系统语音时才按说话人稳定分配不同 voice；否则使用一个自然、中性的系统语音，不得通过改变 pitch 或额外改变用户选择的播放速度伪造人物差异。逐句播放必须保持自然停顿，不得出现机械断裂；第一版不引入付费语音服务。
- 口语识别允许联网，失败时必须降级为录音回放。
- 软件第一版只服务旅游英语学习；水平测试、首日计划和课程起点都以旅游场景所需词汇为主要依据。
- 首次测试只测单词，不测试句子、听力、口语或语法。测试阶段与结果等级分离：每个难度阶段从大型词库中随机抽取约 30 个单词，根据该阶段正确率估算该词库的掌握比例，然后合计各阶段估算词汇量。
- 任一阶段都不要求满分才能继续。20/30、15/30 或其他有效结果都必须计入总词汇量估算并进入后续难度阶段。
- 当前题未作答时直接点击“下一题”，系统必须先把该题记录为“不认识 / 不确定”再进入下一题；用户返回后仍可修改。
- 提交本阶段时不得因存在未答题而阻塞，所有未答题必须先统一记为“不认识 / 不确定”再提交。
- 测试必须提供“剩余全部不会，结束测试”。二次确认后保留已经作答的结果，把当前阶段剩余题和所有后续阶段题目统一记为“不认识 / 不确定”，直接完成测试并生成完整结果。
- 水平测试输出估算旅游英语词汇量、合理区间、各难度阶段正确率和映射等级；不得把内部等级伪装成官方学历、学校成绩或 CET 证书。
- 听力和口语初始状态必须标为“待校准”，在后续真实训练中逐步估算。
- 软件自动安排每天45分钟的有效训练：词汇、听力、口语各15分钟。15分钟是模块的有效训练目标，不是根据现有少量题目预测出来的标签。
- 每个模块在目标有效时间到达前必须持续提供可评分或可明确标记为不可评分的新题；时间到达后允许完成当前题再结束，不得中途截断音频、答题、反馈或录音。
- 有效倒计时只在页面前台且用户正在作答、播放/收听、录音、回放或查看当前题反馈时推进；暂停、锁屏、后台、加载、权限等待、网络等待和超过45秒无操作均不计入。
- 进入每日训练时，词汇、听力、口语三个未完成任务必须同时提供可进入的真实入口；用户想先练哪个就先练哪个，不得设置固定顺序或“尚未轮到”锁定。
- 系统可以根据中断任务、到期复习和学习效果突出显示“建议先做”，但推荐不构成前置条件，用户仍可选择任意其他未完成任务。
- 每日必做任务结束后必须提供“继续训练”，允许从词汇、听力、口语中继续选择，不得把已完成计划做成死胡同。
- 听力选择题在播放结束并进入答题后，每个选项同时显示英文和中文翻译；作答前不显示完整原文。
- 关键词听写必须在作答前明确目标关键词数量、填写顺序和格式，但不得提前暴露答案；提交后显示目标关键词、用户答案和原文对照。
- 每个词汇、听力、口语任务结束后必须展示“正确数 / 总数”、百分比、实际有效练习时间、错误或不可评分数量，并保存为学习事件。
- 口语在识别可用时必须展示目标文本、识别文本和有限内容匹配结果；该结果只表示内容是否匹配，不得冒充专业发音评分。识别不可用时必须明确显示“无法评分”并保留录音回放。
- 第一版只提供 2–4 周旅游英语内容。

## 3. 第一版修订要求（2026-07-27）

### 3.1 首次水平测试

当前活动需求编号为 `R1`，其规则覆盖此前的逐题自适应测试方案：

- 测试范围只包含旅游英语所需单词。题干只展示一个英文单词，不使用句子、音频、图片、语法题或口语题。
- 为了让“全对”可以客观判定，每个英文单词默认提供中文释义选择和“不认识 / 不确定”操作。单纯让用户自报“认识”不能作为正式评分证据。
- 最终结果等级固定为：幼儿园；小学一年级至小学六年级；初中一年级至初中三年级；高中一年级至高中三年级；大学英语四级；大学英语六级。
- 上述名称是本软件内部学习标签。“大学英语四级 / 六级”只代表大致难度参照，不代表通过官方 CET-4 / CET-6 考试。
- 测试阶段不是上述 15 个结果等级。首版把词汇库划分为 5 个由易到难的抽样阶段，每阶段约 30 题，共约 150 题；以后调整阶段数只允许改善抽样精度，不能改变15个结果标签的含义。
- 每个阶段必须拥有显著大于 30 题的独立候选池，首版最低为 150 个有效单词。一次测试从每个阶段均匀随机抽取 30 个，阶段内和本次测试内不得重复。
- 每次新测试必须重新随机抽题和打乱中文选项；词库容量允许时，应优先避开最近一次测试已经出现的单词。退出恢复时必须保留本次已经抽中的题目，不能刷新换题。
- 普通路径中，每个阶段无论正确率是多少都进入下一阶段，直到 5 个阶段全部完成；用户主动确认“剩余全部不会”时，可以提前停止作答，但仍须生成后续阶段全部为不会的完整结果。不得再使用“30/30 才升级”“未满分停止”或“一错就降级”规则。
- 阶段掌握比例按该阶段正确数 / 实际有效题数估算。例如 30/30 估算掌握该阶段 100%，15/30 估算掌握 50%，6/30 估算掌握 20%。
- 每个阶段必须声明其代表的旅游英语词汇数量。总估算词汇量按“各阶段掌握比例 × 该阶段代表词汇数量”分别计算后相加；计算规则、舍入方式和词库版本必须可追溯。
- 选择题存在猜中概率，因此结果只能称为“估算词汇量”。界面必须提供“不认识 / 不确定”，不得强迫猜答案；结果同时给出合理区间，不能把抽样估算伪装成精确事实。
- 阶段提交前允许检查和修改已选答案；提交后固定该阶段结果，不因误触静默改分。
- “下一题”具有明确的默认不会语义：当前题未答时点击下一题，运行时必须以一次可恢复、串行的操作先写入 `uncertain`，写入成功后再导航。界面应提示“未作答将按不会记录”，不能静默丢题。
- “提交本阶段”允许存在未答题。提交动作必须把本阶段所有未答题批量补为 `uncertain` 后再评分和锁定；检查页显示“还有 X 题未答，提交后将按不会记录”，不再禁用提交按钮。
- “剩余全部不会，结束测试”必须出现在检查/提交本阶段操作附近。首次点击进入二次确认，明确显示将被记为不会的剩余题数；确认前可以取消，确认后不可撤销。
- 确认提前结束时，已经回答的题目原样保留；当前阶段剩余题和所有后续阶段的抽样题全部记为 `uncertain`。系统仍生成五个阶段的完整结果、总词汇量、合理区间和15级映射，并记录停止原因为 `remaining-marked-unknown`。
- 提前结束、批量补未答和普通逐题作答必须使用同一估算公式，不得为了快速结束伪造额外正确题、改变代表词数或跳过结果阶段。
- 结果页展示：估算旅游英语词汇量、合理区间、5 个阶段各自正确数 / 30 与掌握比例、总作答数、实际有效时间、映射等级和规则说明。
- 15 个结果等级由估算总词汇量映射，不由某一阶段是否满分决定。映射阈值必须在题库契约中集中定义、版本化并经过边界测试。
- `AbilityProfile` 可以保留三个能力维度以兼容学习引擎，但听力和口语必须为 `unknown` / `pending-calibration`，不能从单词等级推导听力或口语成绩。
- 测试不规定目标时长或最长时长。必须记录实际有效时间，支持保存、退出和继续，并显示当前抽样阶段和总进度。
- 首日计划必须依据映射等级选择保守起点；听力和口语通过后续正常训练校准，不得插入伪装成日常训练的强制入门考试。

### 3.2 每日计划、自由选择和继续训练

- 每日计划中的词汇、听力、口语是三个可独立启动的任务。只要任务真实存在且未完成，进入每日训练时就必须同时可选，用户可以从任意一项开始。
- 不得使用数组位置、`nextTaskId`、前一任务完成状态或“尚未轮到”文案把另外两项锁定。`nextTaskId` 只能表达推荐项，不能表达唯一允许启动的任务。
- 中断任务和到期复习可以被置顶或标记为“建议先做”，但用户可以忽略建议并进入其他未完成任务。已完成任务可以显示完成状态，不再作为每日必做重复启动。
- 任意顺序完成三个任务后都必须得到相同的每日完成结果，先做口语、先做听力或先做词汇不得改变计分、进度保存和后续任务可用性。
- 每日必做计划固定为三个15分钟有效训练任务，总目标45分钟；入口显示“15分钟有效训练”，训练中显示剩余有效时间和已完成题数。
- 15分钟到达前，运行时必须持续从当前等级的新题、错题、到期复习和当天内容变式中续题；不得因单个内容单元只有6题而提前完成。
- 时间到达时设置“本题完成后结束”，当前题评分或明确进入不可评分终态后结束模块；不得为了精确停在900秒而截断音频、输入、反馈或录音。
- 有效练习时间只统计页面前台且用户正在作答、播放/收听、录音、回放或查看当前题反馈的时间；后台、超过45秒无操作、暂停、加载、权限和网络等待不计入。
- 个人历史速度可以保留为分析数据，但不再缩短15分钟目标、改变每日完成条件或导致题目提前耗尽。
- 内容层和运行时必须保证目标时间内有足够题目并优先避免短期重复；确实无法继续供题时必须明确报告内容不足，不能循环同几题或伪造完成。
- 完成每日必做计划后，首页和训练页都必须提供继续训练入口。额外训练优先使用错题、到期复习和当天内容的变式，再使用新的可选内容。
- 额外训练必须记录进度和用时，但不得回滚“今日计划已完成”，也不得伪造新的必做任务。

### 3.3 训练反馈和计分

- 词汇与听力按可判定题目统计正确数和总数。
- 口语只对识别成功且存在明确目标文本的题目做有限内容匹配；显示识别文本、遗漏词、额外词和本题结果。
- 口语识别失败、权限拒绝、离线或录音失败时，本题计入“不可评分”，不能计错，也不能为了凑分数猜测答案。
- 每个任务结束页至少显示：正确数 / 总数、正确率、不可评分数、实际有效时间、主要错误和建议复习项。
- 每日总结必须汇总三个模块，但仍保留各模块独立结果，不能只显示一个模糊总分。

### 3.4 听力答题信息

- 选择题必须在音频播放完成或用户主动进入答题后显示选项；英文为主信息，中文翻译为辅助信息。
- 完整原文和答案解释只在提交后显示，避免把听力题变成阅读抄答案。
- 关键词听写在作答前显示目标数量，例如“填写 3 个关键词”，并使用编号空位或等价的明确结构。
- 界面必须说明是否区分大小写、标点、缩写和词形；默认忽略大小写与句末标点，内容层可声明可接受词形。
- 提交后逐项显示目标关键词、用户输入、正确/错误状态和其在原文中的位置。

### 3.5 实施责任与顺序

本轮只由 `00` 修改总纲，不在本对话实现局部代码。R3已按以下顺序完成并关闭：

1. `04` 定义每个模块900秒有效训练预算、可恢复剩余时间、持续续题、时间到后完成当前题、内容耗尽和完成事件契约；个人速度不改变预算。
2. `05` 提供足够的当前等级新题、错题/复习引用和可追溯变式来源，定义去重窗口；不得靠循环同6题填满15分钟。
3. `06`、`07`、`08` 分别接入连续题流和既有有效计时基础设施；后台、暂停、空闲、加载、权限及网络等待不计入，到时不得截断当前题。
4. `02` 展示15分钟目标、剩余有效时间、累计题数及“本题完成后结束”，不在 UI 自行决定任务完成。
5. `01` 集成、保存倒计时与题目游标、处理刷新/系统中断并重新部署；`09` 验证三模块不会提前耗尽、到时收尾、排除规则、恢复和计分。
6. 用户已在真实 iPhone 确认三项限时训练，`00` 已关闭R3并按问题库顺序激活R4。

R3历史步骤只保留为回归依据，不得重复执行；当前任何任务不得绕过R4转去实现后续排队需求。

## 4. 强制工作协议

所有任务每次工作都必须遵守以下流程：

1. 先判断用户要求是否属于本任务职责。
2. 越界要求必须拒绝，并指出应该进入哪个编号任务。
3. 开始前列出有序步骤，状态只能是：
   - 待开始
   - 进行中
   - 已完成
   - 被阻塞
4. 一次只能有一个步骤处于“进行中”。
5. 每完成一个关键步骤，必须告诉用户：
   - `步骤 X 已完成`
   - `下一步是 Y`
6. 当前进行不下去时，必须立即报告，不能沉默、猜测或假装完成。
7. 依赖其他任务时，不得自己代做或修改对方领域。
8. 未完成测试、存在占位内容、TODO 或未验证关键路径时，不得声称完成。
9. 多个任务共享同一目录。各任务必须遵守本文的文件所有权，不得修改其他任务的专属文件。
10. 发现越界改动时，默认由文件所有者审查、保留有价值内容并迁移到正确位置，不得直接丢弃，也不得把普通工程判断推给用户。
11. 只有本文没有覆盖、且会实质改变产品范围、成本、隐私、安全或数据的冲突，才交给 `00｜项目总纲与全局要求` 裁决。
12. 同一文件正在被另一活跃任务同时修改时，暂停该文件，但可以继续当前活动需求内不冲突的工作；不得转去开发下一条排队需求。

### 单项需求串行门禁

1. 任意时刻只能有一条产品需求处于“活动”状态。一个活动需求可以依次经过多个文件所有者，但这仍是一条需求，不是多个并行功能。
2. 活动需求只有同时满足以下条件才算完成：
   - 所需内容、契约和生产代码真实存在，无占位、TODO 或演示数据；
   - 职责内单元测试、跨模块集成测试和全量检查通过；
   - `01` 已接入真实应用入口并完成生产构建和部署；
   - `09` 已在正式站验证关键路径和旧数据恢复；
   - 涉及真实听感、真机交互或主观可用性时，用户已在实际设备确认。
3. 任一条件未满足，状态必须是“部分完成”或“被阻塞”，所有任务都不得开始下一条产品需求。
4. 上游模块交付只会解锁当前需求的下一个子步骤，不会自动解锁排队中的其他需求。
5. 修复过程中发现属于当前需求的缺陷，必须留在当前需求内修复并重新回归；不得把缺陷登记后继续下一个部分。

### 默认自主决策规则

用户是产品所有者，不是技术项目经理。所有任务必须自己完成职责内的正常工程判断，禁止事事要求用户确认。

以下事项必须自主决定、执行并在结果中说明，不得询问用户：

- 文件如何拆分、命名、迁移和导入。
- 目录所有权范围内的代码组织、重构和集成。
- 已批准技术栈内的库用法、类型设计、错误处理和测试方式。
- 保留并迁移其他任务的越界改动。
- 修复职责内的构建、类型、测试、格式和兼容性问题。
- 在不改变产品行为的前提下选择更安全、简单、可维护的方案。
- 依照本文的依赖顺序判断下一步进入哪个任务。

只有以下情况才允许要求用户确认：

- 增加或删除总纲中的主要功能。
- 产生新的付费、订阅或外部服务依赖。
- 上传个人数据、改变隐私边界或引入账户/云端。
- 删除用户数据、丢弃现有成果或进行难以恢复的操作。
- 两种方案会明显改变用户体验目标，且无法从总纲判断。
- 缺少只有用户本人知道的业务事实，继续猜测会造成方向性错误。

即使允许询问，也必须先给出推荐方案和理由。普通技术选择不得抛给电脑小白。

## 5. 阻塞报告格式

任何任务无法继续时，必须直接使用以下结构：

```text
当前无法继续：卡在步骤 X
阻塞原因：具体事实
需要什么：用户操作、缺失文件或其他编号任务的交付
解锁条件：完成什么以后可以继续
下一步位置：回到哪个任务的哪个步骤
当前可先进行：可安全继续的内容；没有就明确说没有
```

## 6. 完成报告格式

职责内全部验收通过时，必须直接使用以下结构：

```text
本任务已完成
完成内容：
验证证据：
修改文件：
遗留风险：
已解锁任务：
下一步应进入：
```

只完成部分时必须说“部分完成”，不得使用“本任务已完成”。

完成报告是沟通要求，不是依赖门禁本身。下游任务必须以实际文件、公开契约、交付文档和可复现测试为准，不能因为聊天中缺少某句固定措辞而阻塞。

### 依赖门禁的证据规则

依赖是否满足，按以下证据优先级判断：

1. 所需文件和公开契约真实存在。
2. 交付文档说明输入、输出和限制。
3. 相关类型检查、测试和构建可以复现通过。
4. 上游任务的完成通知或步骤通知。

前三项证据已足够时，即使没有格式化完成通知，下游也必须继续，不得要求用户去另一个任务补一句话。

只有出现以下情况才算真实依赖阻塞：

- 必要文件或契约不存在。
- 类型、测试或构建失败。
- 上游产物与当前公开契约不兼容。
- 继续工作必须猜测未定义的业务含义。

依赖审计发现问题时，应指出具体缺失文件、失败测试或不兼容字段。只说“没有完成通知”不构成阻塞。

下游发现真实上游缺口时，按以下方式处理：

1. `00` 直接把缺口派回对应文件所有者，不要求用户充当传话员。
2. 上游只补齐本次缺失的公开契约、内容或测试，不重做已经完成的无关工作。
3. 不同所有权目录的修补可以并行进行。
4. 修补完成并通过验证后，`00` 自动通知下游重新审计并继续。
5. 只有修补会改变总纲、费用、隐私或删除成果时才询问用户。

## 7. 项目任务和边界

### 00｜项目总纲与全局要求

负责：

- 修改项目目标、范围、主要模块和优先级。
- 决定跨任务争议和第二阶段是否启动。
- 修改任务职责与项目级验收标准。

拒绝：

- 页面、算法、课程或代码的局部实现。

### 01｜技术架构与 PWA 底座

负责：

- 技术选型、目录、模块接口、PWA、存储、离线、构建、部署和集成；负责新版能力档案、有效时间、额外训练和任务总结的存储迁移与生产路由。

固定步骤：

1. 核对总纲、环境和目标浏览器。
2. 比较可行技术方案，自主采用最符合总纲的方案并记录理由；只有触发“允许要求用户确认”的条件时才询问。
3. 定义目录、文件所有权、模块契约和数据版本规则。
4. 建立可运行的 PWA 基础项目。
5. 建立本地存储、迁移、错误和权限基础设施。
6. 建立离线缓存、更新和安装能力。
7. 提供模块空壳、契约文档和模拟接口。
8. 集成其他任务的交付及数据迁移，部署并验证 iPhone 安装。
9. 输出技术交接说明。

解锁：

- 步骤 3 完成后，03、04、05 可开始详细设计。
- 步骤 4–7 完成后，02、06、07、08 才能安全实现。
- 步骤 8 必须等待 03–08 交付。

### 02｜UI / UX 与视觉体验

负责：

- 信息架构、页面、导航、视觉、设计系统、展示组件、iPhone 适配和无障碍。R1 活动期间只负责分级单词关卡、进度、提交检查和结果展示；其他排队需求的页面不得提前制作。

固定步骤：

1. 读取 01 的路由、状态和组件边界。
2. 列出当前活动需求需要的页面、状态和导航关系。
3. 比较视觉方向，自主采用最符合日常学习和 iPhone 单手使用的方向；用户之后可提出修改。
4. 建立设计系统和移动端规则。
5. 设计当前活动需求的主要页面；R1 包含阶段说明、30 题答题、未答默认不会提示、提交检查、剩余全部不会二次确认、恢复和分级结果。
6. 设计加载、空白、错误、离线和权限状态。
7. 实现纯展示组件和样式；只接收外部提供的等级、时间、得分、翻译和任务状态。
8. 完成 iPhone 尺寸与无障碍验证并交接。

拒绝：

- 业务算法、课程、存储、部署和训练模块逻辑。

### 03｜初始水平测试与等级系统

负责：

- 旅游英语分阶段随机单词测试、分阶段掌握比例、总词汇量估算、15 级结果映射、旧数据迁移和带待校准维度的能力档案。

固定步骤：

1. 读取 01 的模块契约。
2. 建立 5 个由易到难的抽样阶段、每阶段至少 150 个候选单词，以及幼儿园至大学英语六级的15级结果映射。
3. 定义每阶段随机抽取 30 题、任意正确率继续、未答自动记为不会、提前结束、阶段掌握比例、总词汇量公式和映射阈值契约。
4. 定义英文单词、中文释义选择、不认识/不确定、随机题目/选项和专用题库规范。
5. 设计阶段评分、未答批量补齐、提前结束停止原因、提交前修改、恢复、估算区间、总词汇量和结果等级输出。
6. 实现分阶段抽样估算引擎、未答默认不会和剩余全部不会动作，并输出词汇量与等级已估算、听力/口语待校准的新版能力档案。
7. 验证随机性、同次不重复、重测换题、直接下一题、未答提交、提前结束取消/确认、0%/20%/50%/100%阶段结果、映射边界、重复点击、退出恢复、旧档案迁移和答案不泄露。
8. 向 04、02、01、09 交付 R1 契约和限制。

拒绝：

- 每日学习算法、正式课程、UI、日常训练模块，以及首次测试中的听力或口语评分。

### 04｜学习计划、复习算法与进度

负责：

- 每日计划、用户自由选择与非强制推荐、有效练习时间、个人时长校准、额外训练、间隔复习、掌握度、任务总结、能力进度和学习事件。

固定步骤：

1. 读取 01 的事件契约和 03 的能力档案。
2. 定义掌握度、错误、遗忘、难度、有效时间、正确/不可评分和个人速度指标。
3. 设计词汇、听力、口语各900秒有效训练的任务预算和持续续题状态机，并保证三个真实任务可由用户按任意顺序独立启动；推荐顺序不得成为锁定条件。
4. 设计复习、重试、中断续学、完成后继续训练及额外训练不回滚每日完成态的规则。
5. 定义训练任务、计时、单元结果、额外训练和学习事件接口。
6. 实现计划、复习、时长校准、额外训练和进度引擎。
7. 验证新用户待校准维度、偏科、缺课、任务超时、计划完成后继续训练和时长偏差场景。
8. 向 02、05、06、07、08 交付契约。

拒绝：

- 水平测试评分、课程、UI、音频和语音识别。

### 05｜课程体系与首批内容

负责：

- 2–4 周课程、词汇、句型、例句、带结构化说话人标识的听力脚本、听力选项翻译、关键词听写元数据、固定口语任务和场景测验。

固定步骤：

1. 读取 01、03、04 的内容与等级契约。
2. 确定课程目标、场景顺序和每天负荷。
3. 定义每个场景的词汇、句型、学习目标、选项翻译和关键词听写目标。
4. 编写例句、听力脚本、双语选择项、关键词数量/顺序/可接受答案、口语问答和测验；听力对话的说话人标识与台词文本必须分开保存，人名或角色名不得混入待朗读台词。
5. 检查难度、相关性、翻译准确性、关键词可判定性、标准美式一致性和版权。
6. 生成结构化课程包。
7. 完成内容与跨模块自检。
8. 向 06、07、08、09 交付内容。

拒绝：

- 程序、UI、算法、部署、完整一年课程和 AI 对话。

### 06｜词汇训练模块

负责：

- 词汇题型、判定、正确数/总数、有效时间、任务总结、额外训练、反馈、事件上报和模块实现。

固定步骤：

1. 检查 01、02、04、05 的交付。
2. 明确题型、计时、计分、总结和额外训练输入输出。
3. 实现状态机、判定、正确数/总数、错误摘要和反馈。
4. 接入课程与学习引擎。
5. 接入 UI。
6. 处理恢复、错误、离线、有效时间、额外训练和事件上报。
7. 完成模块测试。
8. 输出交接和限制。

拒绝：

- 扩写内容、全局算法、UI 美化和其他训练模块。

### 07｜听力训练模块

负责：

- 合成语音、按说话人区分音色、播放、速度、重复、逐句训练、双语选择项、明确的关键词听写、正确数/总数、有效时间、任务总结、额外训练和降级。

固定步骤：

1. 检查 01、02、04、05 的交付。
2. 明确题型、播放状态、说话人—音色映射、双语选项显示时机、关键词目标结构、计时、计分和判定；自然度优先于音色区分，同一对话中的同一说话人在使用多音色时必须保持同一 voice。
3. 在目标 Safari 上验证 `en-US` 系统语音的真实自然度、清晰度和可用数量；不得仅凭 voice 数量、名称或参数不同认定可用。
4. 实现按台词排队且停顿自然的播放与控制；界面可以显示人名或角色名，但合成语音只能朗读台词正文。可接受的不同系统 voice 不足时统一使用一个中性 voice，pitch 固定为 1，播放速度只能来自用户选择。
5. 实现题型、双语选项、关键词逐项对照、正确数/总数、有效时间、任务总结、额外训练、反馈和事件上报。
6. 接入 UI、课程、学习引擎和降级。
7. 测试在线、离线、后台、音频中断、不朗读说话人标签、双语选项显示时机、关键词指引与判定、计分、多说话人 voice 稳定性、单一中性 voice 降级、逐句停顿和真实设备听感；参数不同但听感机械或难以使用必须判为失败。
8. 输出交接和限制。

拒绝：

- 编写听力内容、全局算法、UI 美化和其他模块。

### 08｜口语训练模块

负责：

- 麦克风、录音、回放、语音识别、目标/识别文本对照、有限内容匹配、正确/不可评分统计、有效时间、任务总结、额外训练和失败降级。

固定步骤：

1. 检查 01、02、04、05 的交付。
2. 验证 Safari 的麦克风、录音、回放和语音识别。
3. 定义权限、录音、识别、文本对照、有限内容匹配、正确/不可评分、计时、总结、失败和降级状态。
4. 实现录音与回放。
5. 实现识别、目标/识别文本对照、遗漏/额外词提示和有限内容匹配。
6. 实现拒绝权限、离线和识别失败降级。
7. 接入 UI、课程、学习引擎，记录有效时间、任务总结与额外训练，并测试可评分和不可评分路径。
8. 输出交接和限制。

拒绝：

- 开放式 AI、专业发音评分、课程编写和全局算法。

### 09｜测试、质量与第一版验收

负责：

- 测试策略、验收、缺陷证据、回归测试、时间偏差检查和 14 天实测结论。

固定步骤：

1. 读取总纲及 01–08 的交付。
2. 建立需求—测试追踪和通过门槛，覆盖本轮首次词汇测试、待校准维度、每日训练自由选择、有效时间、继续训练、双语选项、关键词指引、口语匹配和任务得分。
3. 准备自动化、集成、端到端、离线、时间偏差和兼容性测试。
4. 分别验收各模块及每日计划完成后的额外训练闭环。
5. 在真实 iPhone 上验证安装、离线、权限、恢复，以及听力对话不朗读说话人标签、逐句衔接自然度、系统语音清晰度、多音色可接受性和单一中性 voice 降级。真实听感是强制门禁，不能用自动化 voice/pitch/rate 参数代替。
6. 报告缺陷并等待责任任务修复后回归。
7. 记录连续 14 天个人使用。
8. 输出通过或不通过结论。

拒绝：

- 直接修复生产代码、改变验收标准或新增功能。

## 8. 文件所有权

文件所有权以目录职责为准。文件所有者负责最终集成和质量，其他任务只能通过公开接口提供成果。

### 00 所有

- `AGENTS.md`
- 项目总纲、范围和跨任务裁决文档

### 01 所有

- 根目录构建与依赖配置：`package.json`、锁文件、`index.html`、`vite.config.*`、`tsconfig*`
- 构建和部署：`.github/**`、PWA 图标与公开资源配置
- 应用入口和集成：`src/main.*`、`src/app/**`
- 通用契约和错误基础设施：`src/core/**`
- 平台能力：`src/platform/**`
- 本地数据：`src/storage/**`
- PWA：`src/pwa/**`
- 全局样式入口 `src/index.css`，但该文件只负责基础重置和导入 `02` 的视觉样式
- 公共测试环境配置：`src/test/setup.*`

### 02 所有

- `src/ui/**`
- 设计系统、主题变量、视觉样式、图标、纯展示组件和界面原型

`02` 不得直接修改 `src/app/**`、`src/core/**`、`index.html` 或 `vite.config.*`。需要接入时，只在 `src/ui/**` 导出组件或主题元数据，由 `01` 集成。

### 03 所有

- `src/features/assessment/**`
- `content/assessment/**`

### 04 所有

- `src/learning-engine/**`

### 05 所有

- `content/curriculum/**`
- `content/lessons/**`

### 06 所有

- `src/features/vocabulary/**`

### 07 所有

- `src/features/listening/**`

### 08 所有

- `src/features/speaking/**`

### 09 所有

- `tests/e2e/**`
- `tests/qa/**`
- QA 报告和测试夹具

功能模块目录内部的单元测试归该模块所有；`09` 只增加外部验收和黑盒测试，不进入模块内部修生产代码。

### 当前 01/02 冲突的最终裁决

- 保留 `02` 已完成的视觉成果，禁止回滚或直接丢弃。
- `src/ui/**` 的现有内容继续归 `02`。
- `src/app/platform-pages.tsx`、`src/core/errors/**`、`index.html`、`vite.config.ts` 的最终版本归 `01`。
- `src/index.css` 中的视觉规则应由 `01` 原样迁移到 `src/ui/styles/**`，`src/index.css` 只保留基础入口和导入。迁移不等于重新设计 UI。
- `01` 可以保留对 `02` 公开组件的导入，并负责路由、错误边界、主题元数据和 PWA 配置的最终集成。
- `src/app/module-registry.ts` 归 `01`。`01` 应补齐可验证的注册机制、契约和测试，不得为了填满数组伪造尚未交付的训练模块。
- `01` 已获得处理上述迁移和集成的权限，不得再次要求用户确认这些普通工程事项。
- `02` 后续只修改 `src/ui/**`。需要共享入口变化时，提供导出接口和集成说明，不直接改 `01` 文件。

## 9. 推荐执行顺序

初始底座、R3、R4、R6和R7已经完成，不得重新执行旧任务。当前只执行R8：

1. `00` 固定口语有限内容匹配边界：只判断用户说出的内容是否与目标表达大致一致，不得宣称专业发音评分。
2. `08` 提供目标文本、真实识别文本、匹配结果和无法识别降级状态；不得伪造识别结果。
3. `02` 只展示上游提供的目标文本、识别文本及有限匹配反馈；`01`负责正式路由、保存和刷新恢复集成。
4. `09` 验证识别成功、部分匹配、不匹配、识别失败、设备不支持、刷新恢复、正式部署和实际入口。
5. 代码、测试、正式部署和用户原入口确认全部完成后，`00`才能关闭R8并激活R9。

每个子步骤以“实际交付物存在、契约可读取、验证可复现”为进入条件；整条R8以“已部署并在实际入口验收”为完成条件。完成通知用于沟通，但不是单独门禁。不得要求用户在任务之间传递完成消息。
